specVersion: "v1"
serviceId: "taskopolis"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "taskopolis-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
